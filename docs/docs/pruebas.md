# Informe de Pruebas

Se realizaron las siguientes pruebas:

- PING: el servidor respondió correctamente.
- LIST: se obtuvo la lista de procesos activos.
- START: se ejecutaron comandos reales como open . y python3 -c "import time; time.sleep(60)".
- STOP: se detuvieron procesos usando su PID.
- INFO: se obtuvieron valores reales de CPU y RAM.

Todas las pruebas se muestran en capturas dentro del reporte.
